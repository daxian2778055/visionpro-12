# 线程安全审计定稿（第43轮 · lock/Invoke 全仓）

> 存档日期：2026-09-26 ｜ 审计方式：静态全量扫描（141 个 .cs，排除 bin/obj）+ 高危锚点人工复核
>
> **行号基准说明**：本报告 Form1.cs 行号为**插入第43轮不变式注释之前**的值；该注释位于原 3756 行处（4 行），
> 故 Form1.cs 中 ≥3757 的所有行号在定稿后**整体 +4**（例：`lock(_locker_open)` 现为 3765，CS0652 基线 13223 → 13227）。
> 其余文件行号不受影响。
>
> **复核说明（第43轮降级）**：1-1（`Form1.cs:3761` `_locker_open`）与 1-2（`Form1.cs:3790` `_close`）已按
> "锁全仓单站点、锁体内无 `Wait`/`Result`/`Join`、委托内不再取锁、无 `MessageBox`"三条证据从【高】降级为【中】
> （残余风险 = 持锁窗口大 + 66 次同步封送的 UI 卡顿面），死锁环静态可证不成立。
> 降级三项硬证据（第43轮实测）：
> 1. `lock (_locker_open)`、`lock (_close)` 全仓各仅 1 个加锁点（Form1.cs:3761 / 3790），且都在后台 Task 链——UI 线程永不取这两把锁；
> 2. 锁体 3761–4377 共 617 行内：无 `Wait()/.Result/.Join()/WaitOne`、无 MessageBox、除这两处外无第二把字面锁；
> 3. 4359 `ArmCommTriggerAfterRunReady()` 取 `_cameraLock` 不在 Invoke 中，4362 的 Invoke 时 `_cameraLock` 已释放——持相机锁等 UI 的交错不存在。
>
> 不变式已作为 4 行注释立于 `Form1.cs` `lock (_locker_open)` 上方（违反任意一条即复活死锁）。

## 处置台账（第43轮结论）

| 项 | 级别 | 第43轮处置 |
|---|---|---|
| 4-2 `Form1.Camera18Events.cs:629` 后台读 `listBox3.SelectedItem` | 中 | ✅ **已修**——UI 线程先快照再进 Task.Run（Camera18Events.cs:623 一带） |
| 1-1 / 1-2 `_locker_open` / `_close` 持锁同步 Invoke | 高→**中** | ✅ **降级定稿** + `Form1.cs` 锁上立不变式注释（0 代码风险） |
| 4-1 Arm 链直读 `cbSoftTriggerN`（ErrorDisplay.cs:292-341） | 高 | 📋 归**台架专场**：修法 = 沿 `Form1.cs:3757` 既有"UI 线程预缓存"模式，进后台前快照 12 路复选框状态数组下传，非顺手改 |
| 3-1 `_sendLock ↔ _cameraLock` 唯一双向反序（B 方向无界） | 中 | 📋 归**台架专场**：方向 B 持相机锁阻塞抢发送锁无上限，需打断无界等待或统一次序 |
| 1-3 ~ 1-8 锁内 Sleep(30/50/80)、`locker_ok` 持锁跨 `IOyanshi` 睡眠 | 中 | 📋 归**台架专场**：方向 = 睡眠/SDK 调用移出锁，或锁内改 TryEnter+重试；启动/IO 命脉，须台架回归 |
| 4-3 检测线程读 `tbExposure[].Text`（Form1.cs:5274→+4） | 中 | ⚠️ 用户明示**曝光路径不动**——转现场核对清单（Debug 校验开启时自动曝光静默失效，量纲核对） |
| 1-9 ~ 1-12、4-4 ~ 4-7 | 低 | 📋 记录不修（低危/死代码/有界等待） |

---

# 一、lock 块内含同步 `Invoke` / `WaitOne` / `Join` / `Thread.Sleep` / `Mutex.WaitOne`

### 1-1 【中】`Form1.cs:3761` — `lock (_locker_open)` 内约 66 处同步 `this.Invoke`

```
3757:            Task.Run(() =>
3758:            {
3761:                lock (_locker_open)
3762:                {
3763:                    DisarmCommTrigger();
3765:                    ResetMyJobOutputs();
3766:                    this.Invoke(new Action(() =>
3768:                        label133.Text = "预备清除流程窗体";
```
锁体跨 3761–4377，内含同步 `this.Invoke` 行号：3766、3772、3792、3805、3817、3828、3845、3849、3869、3875、3883、3888、3891、3912、3918、3926、3931、3934、3954、3960、3968、3973、3976、3996、4002、4010、4015、4019、4039、4045、4053、4058、4061、4081、4087、4095、4100、4103、4123、4129、4137、4142、4147、4162、4168、4176、4181、4186、4201、4207、4215、4220、4225、4240、4246、4254、4259、4264、4279、4285、4293、4298、4303、4315、4336、4341、4362。
**风险：中**（原评【高】，复核降级）— `_locker_open` 全仓仅此 1 个站点、锁体内无 `Wait`/`Result`/`Join`/`MessageBox`、被 Invoke 的委托内不再取锁，故静态可证不成立死锁环；残余风险为 600 余行持锁窗口带来的锁争用，以及 66 次同步封送造成的 UI 卡顿面。

### 1-2 【中】`Form1.cs:3790` — 嵌套 `Task.Run` 内 `lock (_close)` 含同步 `Invoke`

```
3788:                        Task.Run(() =>
3789:                        {
3790:                            lock (_close)
3791:                            {
3792:                                this.Invoke(new Action(() =>
3798:                                    CloseStaleAuxWindows("重运行收尾:");
```
**风险：中**（原评【高】，复核降级）— `_close` 全仓仅此 1 个站点、锁体内无 `Wait`/`Result`/`Join`/`MessageBox`、委托内不再取锁，死锁环不成立；残余风险为持锁同步封送的 UI 卡顿面与 3761/3790 两条链路的锁争用。

### 1-3 【中】`Form1.cs:4359` — 仍在 `lock (_locker_open)` 内调用 `ArmCommTriggerAfterRunReady()`

```
4359:                            ArmCommTriggerAfterRunReady();
```
→ `Form1.ErrorDisplay.cs:149-155`
```
151:            ApplyTriggerModesForOpenedCameras();
152:            _jobs.ClearAllCommTriggerPending();
153:            Thread.Sleep(80);
154:            _jobs.CommTriggerArmed = true;
```
**风险：中** — 持 `_locker_open` 阻塞睡眠 80ms，且 151 还会进 `_cameraLock`（见 3-1）、152 会进 `_sendLock`。

### 1-4 【中】`Form1.cs:13297` — `lock (_cameraLock)` 内 `Thread.Sleep(30)`

```
13297:                            lock (_cameraLock)
13301:                            if (i < isGrabbing.Length && isGrabbing[i])
13303:                                _cameraCtrl.Cameras[i].MV_CC_StopGrabbing_NET();
13304:                                Thread.Sleep(30);
13307:                            nRet = _cameraCtrl.Cameras[i].MV_CC_CloseDevice_NET();
```
**风险：中** — 关相机循环 12 路最坏 360ms 全程持 `_cameraLock`，期间触发线程（TryEnter 200ms）连续丢触发、丢帧采样（TryEnter 50ms）取不到锁。

### 1-5 【中】`Form1.ErrorDisplay.cs:539` — `lock (_cameraLock)` 内 `Thread.Sleep(50)` + 嵌套取 `_sendLock`

```
539:            lock (_cameraLock)
554:            if (IsCameraGrabbing(camIndex))
556:                _cameraCtrl.Cameras[camIndex].MV_CC_StopGrabbing_NET();
558:                Thread.Sleep(50);
571:            nRet = _cameraCtrl.Cameras[camIndex].MV_CC_StartGrabbing_NET();
578:            if (IsCommTriggerMode(_jobs.Myjobs[camIndex].triggerMode))
579:                _jobs.ClearCommTriggerPending(camIndex);
```
**风险：中** — 持 `_cameraLock` 睡眠 + 持 `_cameraLock` 阻塞抢 `_sendLock`（见 3-1 反向边）。

### 1-6 【中/高】`Services/JobService.cs:150 / 161` — `lock(job.locker_ok/locker_ng)` → setter 回调 → `Thread.Sleep` + `lock(locker1)`

```
150:            lock (job.locker_ok)
151:            {
152:                job.outputok2 = b;
153:                job.outputok = (b > 0) ? 1 : 0;
154:            }
```
→ `Myjob.cs:143-151`
```
145:                lock (_outputLock) { _changel1 = value; }
150:                myhandle?.Invoke();          // 委托调用放在锁外部 —— 但仍在外层 locker_ok 内
```
→ `Form1.cs:6264-6269`
```
6264:                        if (_jobs.myjob1.IOyanshi != 0)
6265:                            Thread.Sleep(_jobs.myjob1.IOyanshi);
6269:                            output_camera(1, true);
```
→ `Form1.cs:7130 lock (locker1)`
**风险：中/高** — `myhandle` 只是在 `_outputLock` 外、仍被调用方的 `locker_ok` 罩住；`IOyanshi` 为配置的 IO 延时（可为数百 ms～秒级），整段睡眠 + SDK 写全在 `locker_ok` 内，`ResetMyJobOutputs` 与 timer17 脉冲全部排队。

### 1-7 【中】`Form1.cs:7774 / 7782` — `ResetMyJobOutputs` 持 `locker_ok` 触发同一条回调链

```
7774:                    bool gotOk = System.Threading.Monitor.TryEnter(job.locker_ok, 0);
7775:                    try
7776:                    {
7777:                        job.outputok = 0;         // setter → myhandle?.Invoke() → IOxOK
7778:                        job.outputok2 = -1;
7779:                    }
7780:                    finally { if (gotOk) System.Threading.Monitor.Exit(job.locker_ok); }
7782:                    bool gotNg = System.Threading.Monitor.TryEnter(job.locker_ng, 0);
```
代码自带注释已记录该链（7759-7764："持锁 → 赋值 → 回调里 Thread.Sleep(IOyanshi) → 再置位"）。
**风险：中** — `TryEnter(0)` 虽不阻塞等锁，但拿到锁后回调内的 `Thread.Sleep(IOyanshi)` 与 `lock(locker1)` 仍全部持锁执行，UI 线程被自身回调冻住。

### 1-8 【中】`Form1.Camera58Events.cs:5131/5136/5147/5152/5180/5185` — UI 定时器内成对持锁触发同一回调链

```
5131:                lock (job.locker_ok)
5132:                {
5133:                    job.outputok2 = 1;
5134:                    job.outputok = 1;      // → IO1OK → Thread.Sleep(IOyanshi)
5135:                }
5136:                lock (job.locker_ng)
5137:                {
5138:                    job.outputng2 = 1;
5139:                    job.outputng = 1;
5140:                }
```
（`timer17_Tick`；`checkBox69_CheckedChanged` 5180/5185 同构）
**风险：中** — 在 UI 线程持 `locker_ok` 同步执行 `IOxOK`，`Thread.Sleep(IOyanshi)` 直接冻结界面，且期间检测线程写 `SetOk/SetNg` 全部阻塞。

### 1-9 【低/中】`FormModbus.cs:940` / `FormOmron.cs:914` / `FormModbusRtu.cs:998` — `_ioSync` 内同步写控件

```
FormModbus.cs:940:  if (!System.Threading.Monitor.TryEnter(_ioSync, 1000))
FormModbus.cs:952:      try { msg = op(); }
FormModbus.cs:956:  finally { System.Threading.Monitor.Exit(_ioSync); }
```
`op` 实参（`FormModbus.cs:839` 等）为 `DemoUtils.ReadResultText(..., textBox4)` →
```
DemoUtils.cs:144:  textBox.AppendText( DateTime.Now.ToString(...) + ... );
DemoUtils.cs:113:  OperateResult<byte[]> read = readWrite.Read( addTextBox.Text, ushort.Parse( lengthTextBox.Text ) );
DemoUtils.cs:116:  resultTextBox.Text = "Result：" + ...;
DemoUtils.cs:120:  MessageBox.Show( "Read Failed：" + ... );
```
`FormModbus.cs:1048` 直接把 `BulkReadResultText` 放进锁内。
**风险：低/中** — 持 `_ioSync` 同步 `AppendText`/`.Text=`/`MessageBox.Show`；锁等待已封顶 1s（938 行注释），但弹窗期间轮询/心跳/重连全部排队，且轮询线程持锁时 UI 侧最多冻 1 秒。

### 1-10 【低】`Form1.cs:5719` — `lock (_uiMonitorLock)` 词法上包住 `new Thread(...)`

```
5719:            lock (_uiMonitorLock)
5726:                    _uiMonitorThread = new Thread(() =>
5740:                        try { UI_monitor(); }
5765:                                lock (_uiMonitorLock)
```
**风险：低** — 5765 在新线程体中执行，与 5719 是跨线程而非同线程嵌套，不构成运行时重入死锁；仅词法包裹易误读。

### 1-11 【低】`CommGridUiSink.cs:94` — `lock (_pending)` 内仅 `BeginInvoke`

```
94:            lock (_pending)
95:            {
96:                if (_pending.Count > 0) return;
97:                ScheduleFlush();      // → 47: _dgv.BeginInvoke(new Action(FlushPending))
98:            }
```
**风险：低** — 锁内只有异步 `BeginInvoke`，无同步等待。

### 1-12 【低】`Form3.cs:1939 / 2029` — `lock (locker_1)` 内仅 `SafeUi`（=BeginInvoke）

```
1934:            bool ui_client = SafeRead(() => checkBox3.CheckState) == CheckState.Checked;   // 锁外
1938:            string ui_serverIp = SafeRead(() => comboBox1.SelectedItem ...);               // 锁外
1939:            lock (locker_1)
1943:                    SafeUi(() => textBox8.Text = aa);        // 598: if (IsHandleCreated) BeginInvoke(a);
```
（2029 同构，2033/2056/2059 同）
**风险：低** — 同步 `SafeRead` 均在锁外；锁内只有异步投递。

**已核查为干净（锁内无 Invoke/Sleep/Join/WaitOne）：** `ClassIni.cs` `_cacheLock`×15、`RunLog.cs` 55/133/250、`Program.cs:30`、`FormCommManager.cs` 818/833、`CommLinkManager.cs`×7、`ServiceProvider.cs`×6、`AppHost.cs:31`、`SerialPortGuard.cs`×3、`LogManager.cs`×3、`AsyncFileLogger.cs`×5（`Join` 在 148 行锁外）、`RateLimitedLog.cs`×3、`InspectionLifecycle.cs`×6、`CommGridHelper.cs` 204/249、`Form3.cs` 70/867、`Myjob.cs` 145/159、`JobService.cs` 85/92/114、`PendingCameraTriggers.cs` 29/35/47/63/71/83、`Form1.cs` 152/4544/4580/4640/4657/6236/7119–7415/7548/7726/8072/8438/8530/12601/12691/12864/12902/13045/13168/13326、`Form1.ErrorDisplay.cs` 196/297/486、三协议 `_ioSync` 其余锁体、`FormModbus*.cs`/`FormOmron.cs` `_childLinksSync`/`_midValueRegexCache`。

---

# 二、不恰当的锁对象

对全仓 171 条 `lock (` 语句逐一匹配 `lock (this` / `lock (typeof` / `lock ("字符串字面量")`：

**结果：0 处，本节无可报告项。**

锁对象全部为私有 `readonly object` 字段、静态对象或 `Myjob.locker_ok/locker_ng` / `locker1..12` 等专用字段，无暴露于外部的锁目标。

---

# 三、嵌套锁顺序（含反向配对证据）

## 3-1 【中】唯一真正的双向反向对：`_sendLock` ↔ `_cameraLock`

**方向 A（持 `_sendLock` → 抢 `_cameraLock`，有界 200ms）**

`Services/PendingCameraTriggers.cs:44`
```
44:            lock (_sendLock)
46:                LinkedListNode<CameraTriggerRecord> node;
56:                    int result = send();      // = TriggerSoftwareCallback = SendSoftwareTrigger
```
→ `Form1.ErrorDisplay.cs:193-209`
```
196:                Monitor.TryEnter(_cameraLock, 200, ref lockTaken);
197:                if (!lockTaken) { ... return -1; }
204:                return cam.MV_CC_SetCommandValue_NET("TriggerSoftware");
208:                if (lockTaken) Monitor.Exit(_cameraLock);
```

**方向 B（持 `_cameraLock` → 阻塞抢 `_sendLock`，无界）**

- `Form1.ErrorDisplay.cs:578-579`：`lock(_cameraLock)`（539 起）内 `_jobs.ClearCommTriggerPending(camIndex)`
- `Form1.cs:12614 / 12629 / 12637 / 12645 / 12672 / 12679 / 12696`：`lock(_cameraLock)`（12601 / 12691 起）内 `ClearCameraSlotOnOpenFailed(...)` → `Form1.ErrorDisplay.cs:447 _jobs.ClearCommTriggerPending(slot);`
- `Form1.cs:4359` 上游的 `ApplyTriggerModeToCameraHardware` 内 `Form1.ErrorDisplay.cs:345 _jobs.ClearCommTriggerPending(slot);`（297 `lock(_cameraLock)` 内）

以上均落到 `Services/JobService.cs:109-115`
```
113:                _pendingTriggers[slot].Clear();          // → PendingCameraTriggers.cs:82
114:                lock (_continuousParameters[slot]) ...   // 第三层锁
```
→ `PendingCameraTriggers.cs:82`
```
82:            lock (_sendLock)
83:                lock (_sync) _pending.Clear();
```

**风险：中** — 方向 B 无超时上限（`_cameraLock` 持有方可达秒级：13304 关机循环 12×30ms、ErrorDisplay 558 睡眠、重连 SDK 调用），方向 A 有界 200ms 会快速失败返回 `-1`。因此不是双端无限互等，但 B 方向会把触发/回执清理路径整体拖住，形成"持相机锁等发送锁、发送锁又在等相机锁"的环，是全仓唯一的成对反序。

## 3-2 单向链（无反向，未构成环，仅列事实）

| 链 | 证据 | 反向是否存在 |
|---|---|---|
| `_locker_open → _sendLock` | `Form1.cs:3761 lock(_locker_open)` → 3763 `DisarmCommTrigger()` → `ErrorDisplay.cs:146 _jobs.ClearAllCommTriggerPending()` → `JobService.cs:113` → `PendingCameraTriggers.cs:82 lock(_sendLock)` | 否：`_sendLock` 持有者（44/82）体内只进 `_sync` 与 `_cameraLock` |
| `_locker_open → _cameraLock` | `Form1.cs:4359`（仍在 3761 锁内）→ `ErrorDisplay.cs:151 ApplyTriggerModesForOpenedCameras()` → `ErrorDisplay.cs:430` → `Form1.ErrorDisplay.cs:297 lock(_cameraLock)` | 否：`_cameraLock` 全部站点（ErrorDisplay 297/486/539、Form1 8072/8438/8530/12601/12691/12864/12902/13168/13297）体内均不取 `_locker_open` |
| `_locker_open → _locker_ok/_locker_ng` | `Form1.cs:3765 ResetMyJobOutputs()` → `Form1.cs:7774/7782 Monitor.TryEnter(job.locker_ok/locker_ng,0)` | 否：`locker_ok` 持有者体内只进 `_outputLock` 与 `locker1` |
| `_sendLock → _sync` | `PendingCameraTriggers.cs:44→47`、`82→83` | 否：纯 `_sync` 站点 29/35/71 不取 `_sendLock`，全仓顺序一致 |
| `locker_ok → _outputLock` | `JobService.cs:150` → `Myjob.cs:145 lock(_outputLock)` | 否：`_outputLock` 仅存在于 setter/getter 内部 |
| `locker_ok → locker1..12` | `JobService.cs:150` → `Myjob.cs:150 myhandle?.Invoke()` → `Form1.cs:6269 output_camera(1,true)` → `Form1.cs:7130 lock(locker1)`（`output_camera1ng..12` 同构 7161/7181/7211/7241/7271/7301/7320/7339/7358/7377/7396/7415） | 否：`locker1..12` 体内仅 SDK `MV_CC_Set*` 调用，不取任何其它锁 |
| `_locker_open → _close` | 词法上 `Form1.cs:3761` 包住 3788 `Task.Run`，其体 3790 `lock(_close)` | 否，且非运行时嵌套：3790 在池线程执行，两把锁全仓各只有 1 个站点，永不交叉 |
| `_cameraLock → _sendLock`（即 3-1 方向 B） | 见 3-1 | 是 → 与 3-1 方向 A 构成反序 |
| `_childLinksSync` vs `_ioSync` | 全仓无任一站点互相嵌套（`FormModbus.cs` 1619/1624/1664/1674、`FormOmron.cs` 111/118/125/226、`FormModbusRtu.cs` 159/164/204/214） | — |
| `m_BufForSaveImageLock[i]` | `Form1.cs:7548/7726/13045/13326` 各自独立、体内无嵌套 | — |

---

# 四、跨线程访问控件且**未**包 `Invoke`（已按 `// 收口到 UI 线程` / `BeginInvoke` / `SafeBeginInvoke` / `SafeUi` / `TryBeginInvoke` 过滤）

## 4-1 【高】`Form1.ErrorDisplay.cs:301 / 314-315 / 327 / 341` — 后台运行链路直读直写复选框/按钮

`Form1.ErrorDisplay.cs:292-341`：
```
292:        private bool ApplyTriggerModeToCameraHardware(int slot)
297:            lock (_cameraLock)
301:                bool softChecked = GetSoftTriggerChecked(slot);
314:                    SetSoftTriggerEnabled(slot, false);
315:                    SetTriggerExecEnabled(slot, false);
327:                            SetTriggerExecEnabled(slot, nRet == MyCamera.MV_OK && IsCameraGrabbing(slot));
341:                    SetSoftTriggerEnabled(slot, true);
```
被调用的三个 helper 无任何 Invoke 包裹：
```
228:        private bool GetSoftTriggerChecked(int slot)
232:                case 0: return cbSoftTrigger1.Checked;      // …243: cbSoftTrigger12.Checked
248:        private void SetSoftTriggerEnabled(int slot, bool enabled)
252:                case 0: cbSoftTrigger1.Enabled = enabled;   // …263: cbSoftTrigger12.Enabled
267:        private void SetTriggerExecEnabled(int slot, bool enabled)
271:                case 0: bnTriggerExec1.Enabled = enabled;   // …282: bnTriggerExec12.Enabled
```
后台可达链：`Form1.cs:3757 Task.Run` → `Form1.cs:3761 lock(_locker_open)` → `Form1.cs:4359 ArmCommTriggerAfterRunReady()` → `Form1.ErrorDisplay.cs:151 ApplyTriggerModesForOpenedCameras()` → `Form1.ErrorDisplay.cs:430 ApplyTriggerModeToCameraHardware(i)`。
**风险：高** — 检测/运行线程在持 `_locker_open`+`_cameraLock` 下直读 `cbSoftTriggerN.Checked`、直写 `.Enabled`；`CheckForIllegalCrossThreadCalls=true`（Debug）下必抛并中断 Arm，Release 下为无同步的数据竞争。UI 安全调用方为 8426/8521（`this.Invoke` 内）、12725（`bnOpenCore`，UI 线程）、ErrorDisplay 401/414（ComboBox 处理器）。

## 4-2 【中】`Form1.Camera18Events.cs:629` — `Task.Run` 内读 `listBox3.SelectedItem`

```
623:        private void listBox3_MouseDown_1(object sender, MouseEventArgs e)
625:            Task.Run(() =>
626:            {
627:                try
628:                {
629:                    string[] time111 = listBox3.SelectedItem.ToString().Split(':');
635:                    myProc = Process.Start(_jobs.myjob2.pathhead_ng + ...);
```
**风险：中** — 后台线程读 `SelectedItem`（UI 控件属性）无 Invoke；与 UI 线程改选中项竞争，且句柄释放期间会抛 `ObjectDisposedException`（被 642 catch 吞掉，图片打不开且无提示）。
**第43轮处置：✅ 已修** — UI 线程（MouseDown 回调内）先取 `SelectedItem` 字符串快照，再进 `Task.Run`（Camera18Events.cs 623 一带）。

## 4-3 【中】`Form1.cs:5274` — 检测线程读 `tbExposure[camIdx].Text`

```
5265:                            if (runOk && myjob.zidongbaoguang && camIdx >= 0 && ... )
5273:                                string exposureText;
5274:                                try { exposureText = tbExposure[camIdx].Text; }
5275:                                catch { exposureText = "1000"; }
5279:                                Task.Run(() => { ... MV_CC_SetFloatValue_NET("ExposureTime", ...); });
```
**风险：中** — `getrecord` 检测线程跨线程读 TextBox 属性，仅靠 try/catch 兜底；Debug 校验开启时恒走 1000 默认值，自动曝光补偿静默失效。
**第43轮处置：⚠️ 不动码（用户明示曝光路径不动）** — 转现场核对清单。

## 4-4 【低】`Form1.cs:1294 / 12076 / 12303` — 后台线程写 `Frm2.start`

```
1291:                if (qiehuanzhong == 1)
1293:                    _logger.WriteLog("initialize_FormSet 超时未执行，兜底解除阻塞");
1294:                    Frm2.start = 1;
1295:                    qiehuanzhong = 0;
```
```
12075:                            if (_comm.Omron.qiehuanzhong == 0)
12076:                                Frm2.start = 1;
```
```
12301:                        if (_comm.Omron.qiehuanzhong == 0)
12302:                        {
12303:                            Frm2.start = 1;
```
（1294 在后台初始化线程；12076/12303 在 `xinghao_qiehuan` 的 `Task.Run`（11216）体内）
**风险：低** — `Form2.cs:20 public int start;` 是公开字段而非控件属性，不会触发跨线程检查，但与 `Form2` 的 UI 读取之间无内存屏障/同步。

## 4-5 【低】`Form1.cs:456-467` — 后台线程写 `Frm2._progressValue` / `Frm2._statusText`

```
456:        private void UpdateSplashProgress(int value, string status)
458:            try
460:                if (Frm2 != null)
462:                    if (value >= 0 && value <= 100)
464:                        Frm2._progressValue = value;
466:                        Frm2._statusText = status ?? "";
469:            catch { }
```
后台调用点：`Form1.cs:869 / 879 / 895（初始化线程）`、`Form1.cs:1266 / 1297 / 1309（后台初始化线程）`、`Form1.cs:11263 / 11288 / 11302（Task.Run 11216）`、`Form1.cs:12058 / 12106 / 12110（Task.Run 内）`。
**风险：低** — `Form2.cs:17-18` 声明为 `public volatile int/ string`，有 volatile 保障且非控件属性，仅为字段级跨线程写。

## 4-6 【低】`CommGridUiSink.cs:39 / 44 / 46` — 后台线程读 `_dgv` 控件属性

```
33:        private void ScheduleFlush()
35:            if (Interlocked.CompareExchange(ref _invokeScheduled, 1, 0) != 0) return;
39:                if (_dgv.IsDisposed)
44:                if (_dgv.InvokeRequired)
46:                    if (_dgv.IsHandleCreated)
47:                        _dgv.BeginInvoke(new Action(FlushPending));
```
由 `CommGridHelper.SetPollCell`（`CommGridHelper.cs:343 sink.Queue(...)`）从三协议轮询/回写线程调用。
**风险：低** — 三个属性本身不做消息封送，但属无 Invoke 的跨线程控件访问；实际的 `cell.Value` 写入（`FlushPending` 60-92 行）已正确在 UI 线程。

## 4-7 【低 / 死代码】`Form1.cs:8678-8681` — `RecvInfo` 直写 `textBox1.Text`

```
8678:        private void RecvInfo(string str)
8679:        {
8680:            textBox1.Text = str;
8681:        }
```
**风险：低** — 全仓无任何调用点（0 callers），属死代码，仅在被未来引用时才成为跨线程隐患。

---

**已核查并排除（已收口或本质安全）：** `UI_monitor`（5785，`TryBeginInvoke` 5798/5825）、`getrecord`/`GetRecordCore`（`SafeBeginInvoke` 5356/5664，`TryBeginInvoke` 5511/5523）、`ImageCallBack`/`ProcessImageCallback`（13011-13118，无控件访问）、`InspectWorker`（4574）、`zhenlv_1`（2929，用缓存 `_cachedNumericUpDown6Value`）、`InitializeJobManager`（1039 `SafeBeginInvoke`）、`trriger_set`（`Form1.DisplaySettings.cs:692 this.Invoke`）、`DataChange`（8691 `SafeBeginInvoke`）与 `DataChange_fins/modbustcp/modbusrtu`（8986/9086/9184 + 8931/8956/9029/9061/9129/9159 "收口到 UI 线程"）、`daoqi_jiankong`（`Form1.DisplaySettings.cs:1442/1559 this.Invoke`）、`StartAuthClockSettleRetry`/`StartAuthRestoreRetry`（3232 `this.Invoke`）、`Form3` `chushihua`/`Listen`/`clientmonitor`/`Receive`（`SafeRead`/`SafeUi`）、`RenderCameraFrame`、`MarkRouteListSlotUnbound` 12009（在 11415 `this.Invoke` 内）、`chatu_fangfa`（`Form1.DisplaySettings.cs:1340`，`list_time` 为调用方在 UI 线程取的字符串快照）、`ManualGuarded` 全部 77 个调用点（按钮事件，UI 线程）、`xie`/`ReadLocked`/心跳 `QueueUserWorkItem`（`FormModbusRtu.cs:3601`、`FormOmron.cs:2083`、`FormModbus.cs:2251`：锁内只做 I/O + `CommGridHelper.SetPollCell` 队列写）、`TryAutoReconnect`（`FormModbusRtu.cs:3415`、`FormOmron.cs:1908`：锁内 `PerformReconnectCore` 仅 SDK/串口）、`Services` 三个 `*Runtime` 轮询线程与三个 `*LinkContext.OnReconnect`（全无控件访问）、`Form1.cs:5304/5403/5575`（纯统计/存图）、`SetCameraGrabbing`（`ErrorDisplay.cs:100`，仅写 bool 字段）、`Form1.cs:11207/11339`（在 `xinghao_qiehuan` 的 UI 线程前段 / `this.Invoke` 内）。

---

# 五、统计表

## 5.1 总量

| 指标 | 计数 | 说明 |
|---|---|---|
| `lock (` 语句总数 | **171** | 正则 `\block\s*\(` 命中 176 行，扣除 5 行纯注释：`Form1.cs:110`、`FormModbus.cs:914`、`FormModbusRtu.cs:972`、`FormOmron.cs:888`、`DemoUtils.cs:129` |
| 含 `BeginInvoke` 字样行数（`BeginInvoke` 子串，含 `SafeBeginInvoke`/`TryBeginInvoke`/注释） | **125** | 其中裸 `BeginInvoke`（`\bBeginInvoke\b`）仅 **35** 行 |
| 含 `\bInvoke\(` 行数（非 BeginInvoke 的同步 Invoke，含委托 `?.Invoke()` 与接口声明） | **155** | 其中 `Myjob.cs:150/163`、`CommGridHelper.cs:41`、`FormFinsLinkConfig.cs:205`、`FormModbusRtuLinkConfig.cs:184`、`FormModbusTcpLinkConfig.cs:178`、`Form1.cs:9194` 为委托调用而非控件封送 |

## 5.2 `lock (` 按文件 Top 10

| 排名 | 文件 | `lock (` 数 |
|---|---|---|
| 1 | `Form1.cs` | 36 |
| 2 | `FormModbus.cs` | 16 |
| 2 | `FormModbusRtu.cs` | 16 |
| 4 | `FormOmron.cs` | 15 |
| 4 | `ClassIni.cs` | 15 |
| 6 | `Services\PendingCameraTriggers.cs` | 8 |
| 7 | `Services\CommLinkManager.cs` | 7 |
| 8 | `Core\Threading\InspectionLifecycle.cs` | 6 |
| 8 | `Core\Infrastructure\ServiceProvider.cs` | 6 |
| 8 | `Form1.Camera58Events.cs` | 6 |

（合计 118 / 171，其余 53 条分布于 `CommGridUiSink` 3、`Form1.ErrorDisplay` 3、`CommGridHelper` 2、`Form3` 4、`Myjob` 2、`JobService` 5、`SerialPortGuard` 3、`RunLog` 3、`FormCommManager` 2、`LogManager` 3、`AsyncFileLogger` 5、`RateLimitedLog` 3、`Program` 1、`AppHost` 1 等 15 个文件）

## 5.3 分节结果汇总

| 节 | 结论 |
|---|---|
| 一、锁内同步 Invoke/Sleep/WaitOne/Join | **12 项**（中 9 / 低中 1 / 低 2；1-1、1-2 经复核由【高】降级为【中】） |
| 二、不恰当锁对象（`this`/`typeof`/字符串） | **0 项** |
| 三、嵌套锁反序 | **1 组双向对**（`_sendLock` ↔ `_cameraLock`，中）；另有 8 条单向链，均无反向配对 |
| 四、跨线程裸访问控件（未收口） | **7 项**（高 1 / 中 2 / 低 4） |
